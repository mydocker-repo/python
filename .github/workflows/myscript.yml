name: RunPuppeteer

on:
  # push:
  #   branches:
  #     - main
  #     - master
  #   tags:
  #     - 'v*'
  # pull_request:
  #   branches:
  #     - main
  #     - master
  workflow_dispatch:  # 手动触发

jobs:
  build:
    runs-on: ghcr.io/hillweb/docker-python:latest
    permissions:
      packages: write
    steps:
      - name: Checkout 代码
        uses: actions/checkout@v4

      - name: 运行程序
        run: |
          python main.py
